import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  itemsContainer: {
    flex: 1,
    backgroundColor: '#A5C9CA',
    paddingTop: 30,
  },
  itemsFlatList: {
    gap: 15,
  },
});